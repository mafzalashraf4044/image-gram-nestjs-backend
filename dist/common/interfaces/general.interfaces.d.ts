export interface MongoDBConnectionConfig {
    host: string;
    port: number;
    username: string;
    password: string;
    database: string;
}
export interface AWSConfig {
    accessKeyId: string;
    secretAccessKey: string;
}
